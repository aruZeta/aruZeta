```nim
defUser "aruZeta":
  knownLags = @["nim", "common-lisp", "haskell",
                "nix", "c", "c++", "assembly",
                "javascript", "java", "python"]

  favoriteLangs = @["nim", "common-lisp", "haskell"]

  automate = true
```
